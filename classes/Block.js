(function (window) {
    var x;
    var y;

    function Block(x, y, imgTile) {
        this.initialize(SolidName, imgSolid, x_end);
    }
    Block.prototype = new createjs.BitmapAnimation();

    window.Block = Solid;
} (window));